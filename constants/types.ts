export type Work = {
  title: string;
  company: string;
  description: string;
  duration: string;
};

export type Project = {
  projectName: string;
  lineUrl: string;
  description: string;
  tags: string[];
  github: string;
};
